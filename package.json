{
  "name": "upstash-cloud-console",
  "version": "1.0.0",
  "type": "module",
  "private": true,
  "scripts": {
    "start": "bun --cwd backend src/index.js",
    "dev:backend": "bun --cwd backend src/index.js",
    "dev:frontend": "bun --cwd frontend run dev",
    "build": "bun --cwd frontend run build"
  }
}