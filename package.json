{
  "name": "upstash-provisioner",
  "version": "1.0.0",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "start": "bun server.js",
    "dev": "bunx kill-port 4000 && bun --watch server.js",
    "build": "npx puppeteer browsers install chrome",
    "postinstall": "npx puppeteer browsers install chrome",
    "test-redis": "bun test_redis.js"
  },
  "dependencies": {
    "@fastify/compress": "^8.0.1",
    "@fastify/cors": "^10.0.1",
    "@fastify/helmet": "^13.0.1",
    "fastify": "^5.2.1",
    "ioredis": "^5.4.1",
    "puppeteer": "^24.1.0"
  }
}