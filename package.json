{
  "name": "upstash-cloud-console",
  "version": "1.0.0",
  "type": "module",
  "private": true,
  "scripts": {
    "start": "bun --cwd backend server.js",
    "dev:backend": "bun --cwd backend server.js",
    "dev:frontend": "bun --cwd frontend run dev",
    "build": "bun --cwd frontend run build"
  }
}